# Google Places API Configuration
# Get your API keys from: https://console.cloud.google.com/apis/

# New Google Places API (cost-optimized)
EXPO_PUBLIC_GOOGLE_PLACES_API_KEY=your_new_places_api_key_here

# Google Maps API (fallback for legacy support)
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your_maps_api_key_here

# OpenAI API Configuration (for AI-generated tasks)
# Get your API key from: https://platform.openai.com/api-keys
EXPO_PUBLIC_OPENAI_API_KEY=your_openai_api_key_here

# Optional: OpenAI Model Selection (default: gpt-4o-mini)
# EXPO_PUBLIC_OPENAI_MODEL=gpt-4o-mini

# Optional: For production/staging environments
# EXPO_PUBLIC_API_ENV=development

# ======================================
# Configuration Notes:
# ======================================
# 
# GOOGLE PLACES API SETUP:
# 1. Go to https://console.cloud.google.com/apis/
# 2. Enable "Places API (New)" for best results
# 3. Enable "Maps JavaScript API" for fallback support
# 4. Create API key and restrict it to your app bundle/domain
# 5. Set quotas to prevent unexpected charges
#
# OPENAI API SETUP:
# 1. Go to https://platform.openai.com/api-keys
# 2. Create API key and add billing information
# 3. Start with gpt-4o-mini model (cost-effective)
# 4. Monitor usage at https://platform.openai.com/usage
#
# COST OPTIMIZATION:
# - App includes intelligent caching to reduce API calls
# - Location data cached for 24 hours
# - AI responses cached during session
# - Graceful fallbacks if APIs are unavailable
#
# PRIVACY:
# - Location data never stored or transmitted
# - API calls are anonymized
# - All data processing happens locally